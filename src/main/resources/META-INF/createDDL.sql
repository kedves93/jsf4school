CREATE TABLE T_USERS (ID INTEGER NOT NULL, ISADMIN SMALLINT DEFAULT 0, PASSWORD VARCHAR(255), USERNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE T_SURVEYS (ID INTEGER NOT NULL, DESCRIPTION VARCHAR(255), ISCOMPLETED SMALLINT DEFAULT 0, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE T_QUESTIONS (ID INTEGER NOT NULL, TEXT VARCHAR(255), SURVEY_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE T_ANSWERS (ID INTEGER NOT NULL, TEXT VARCHAR(255), VALID SMALLINT DEFAULT 0, QUESTION_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE T_QUESTIONS ADD CONSTRAINT TQUESTIONSSURVEYID FOREIGN KEY (SURVEY_ID) REFERENCES T_SURVEYS (ID)
ALTER TABLE T_ANSWERS ADD CONSTRAINT TANSWERSQUESTIONID FOREIGN KEY (QUESTION_ID) REFERENCES T_QUESTIONS (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
